import { Timeframe } from "../components/TimeframeSelector/TimeframeSelector";

export const FetchData = (timeframe: Timeframe, tickerId: string, datasetId: string) => {
    switch (datasetId) {
        case "":
            break;
    }
}